<!--===================================================================
    ResolutionWindow (tOrder)

    Standalone window hosting the ResolutionControl user component.
    Intended for testing layout scaling, resolution presets and DPI.
====================================================================-->
<Window
    x:Class="tOrder.ResolutionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder"
    xmlns:common="using:tOrder.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
   
    Title="🖥️ Resolution Tester"

>
    <!-- Mica backdrop for modern WinUI look -->
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!--je potřeba použít user control - resourcy-->
    <local:ResolutionControl x:Name="ResControl"/>
</Window>
