<!--
===========================================================================
🪟 MainWindow.xaml
===========================================================================

MainWindow slouží jako hostitelské okno aplikace tOrder. Zobrazuje `MainLayout`,
který obsahuje:
- Navigační panel (NavigationView),
- Horní panel (TopBar),
- Hlavní obsahový rámec (Frame),
- Alert notifikace.

Tento soubor obsahuje pouze jeden vizuální prvek `MainLayout`.
Veškeré UI a logika jsou delegovány na něj.
===========================================================================
-->

<Window
    x:Class="tOrder.Shell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:tOrder.Shell"
    xmlns:navigation="using:tOrder.Shell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Root Layout hostující celou vizuální strukturu aplikace -->
    <navigation:MainLayout x:Name="RootLayout" />

</Window>
