<!--===================================================================
    TopBar (tOrder.Shell)

    Application header component containing:
    ┌──────────────────────────────────────────────────────────────────────┐
    │ 1. Hamburger menu (toggles NavigationView)                 │
    │ 2. Back button (optional)                                  │
    │ 3. Page title and breadcrumb                               │
    │ 4. User info (name, session time, photo)                   │
    │ 5. Notifications icon (alerts / messages)                  │
    └───────────────────────────────────────────────────────────────┘
    Intended to be placed inside a Viewbox and scaled via LayoutConfigVM.
====================================================================-->

<UserControl
    x:Class="tOrder.Shell.TopBar"
    x:Name="TopBarControl"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.Shell"
    xmlns:common="using:tOrder.Common"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d"
    d:DesignWidth="1024"
    d:DesignHeight="68"
    d:DataContext="{d:DesignInstance Type=local:TopBarVM, IsDesignTimeCreatable=True}"

    x:DataType="local:TopBarVM"

    Width="Auto"
    Height="Auto"
    Background="Transparent"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Top"

    common:DesignProperties.Control="TopBarControl"
    common:DesignProperties.Position="Top"
    common:DesignProperties.GroupTag="Layout"
    common:DesignProperties.GroupSubTag="Header"
    common:DesignProperties.DesignWidth="1024"
    common:DesignProperties.DesignHeight="68"
    ToolTipService.ToolTip="TopBarControl">

    <UserControl.Resources>

        <!-- 🎯 Page Heading -->
        <Style x:Key="TopBarHeadingTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- 📍 Breadcrumb -->
        <Style x:Key="TopBarBreadcrumbTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- 🎨 Shared icon style for TopBar -->
        <Style x:Key="TopBarIconStyle" TargetType="FontIcon">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!-- 🎨 Brushes -->
        <SolidColorBrush x:Key="TopBarBackgroundBrush" Color="#003f7f"/>
        <SolidColorBrush x:Key="UserInfoBackgroundBrush" Color="#052f60"/>
        <SolidColorBrush x:Key="DisabledBorderBrush" Color="#88CCCCCC"/>

        <!-- 📐 Layout Constants -->
        <Thickness x:Key="TopBarGridPadding">8,0</Thickness>
        <x:Double x:Key="TopBarColumnSpacing">20</x:Double>
        <Thickness x:Key="UserInfoPadding">8,4</Thickness>
        <CornerRadius x:Key="DefaultCornerRadius">4</CornerRadius>
        <Thickness x:Key="NotificationButtonPadding">4</Thickness>

        <!-- 👤 User Info Sizes -->
        <x:Double x:Key="UserPictureSize">36</x:Double>
        <x:Double x:Key="UserInfoFontSize">12</x:Double>
        <x:Double x:Key="UserInfoSecondaryFontSize">10</x:Double>


        <!--=============================================================-->
        <!-- 🌍 [Localization Keys for TopBar]                           -->
        <!--=============================================================-->
        <x:String x:Key="TopBar_BackButton.ToolTip">Zpět</x:String>
        <x:String x:Key="TopBar_NotificationButton.ToolTip">🔔 Oznámení jsou aktuálně nedostupná</x:String>
        <x:String x:Key="TopBar_HamburgerButton.ToolTip">Zobrazit hlavní menu</x:String>
        <x:String x:Key="TopBar_PageTitlePanel.ToolTip">Informace o aktuální stránce</x:String>
        <x:String x:Key="TopBar_UserInfo.ToolTip">Přihlášený uživatel</x:String>
        <x:String x:Key="TopBarControl.ToolTip">Hlavičkový panel aplikace</x:String>

        <x:String x:Key="TopBar_DefaultHeading">Neznámá stránka</x:String>
        <x:String x:Key="TopBar_DefaultBreadcrumb">/ neznámá</x:String>
        <x:String x:Key="TopBar_UserName">Jméno</x:String>
        <x:String x:Key="TopBar_UserSurname">Příjmení</x:String>
        <x:String x:Key="TopBar_SessionDuration">Doba přihlášení</x:String>
        <x:String x:Key="TopBar_UnknownUser">Nepřihlášen</x:String>
        <x:String x:Key="TopBar_NoSession">–:––</x:String>

    </UserControl.Resources>
    <!--===========================================================
    TopBar layout container (Header strip for app shell)
=============================================================-->
    <Border x:Name="TopBarBorder"
        Background="{StaticResource TopBarBackgroundBrush}"
        VerticalAlignment="Top">

        <!--=======================================================
        TopBar content Grid – contains all interactive sections
    =========================================================-->
        <Grid x:Name="TopBarGrid"
          Padding="{StaticResource TopBarGridPadding}"
          ColumnSpacing="{StaticResource TopBarColumnSpacing}"
          ToolTipService.ToolTip="{StaticResource TopBarControl.ToolTip}"
          common:DesignProperties.DesignHeight="68"
          common:DesignProperties.GroupTag="Layout"
          common:DesignProperties.GroupSubTag="Header">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Hamburger -->
                <ColumnDefinition Width="Auto"/>
                <!-- Back button -->
                <ColumnDefinition Width="Auto"/>
                <!-- Reserved -->
                <ColumnDefinition Width="*"/>
                <!-- Page title -->
                <ColumnDefinition Width="Auto"/>
                <!-- Reserved -->
                <ColumnDefinition Width="Auto"/>
                <!-- User info -->
                <ColumnDefinition Width="Auto"/>
                <!-- Notifications -->
            </Grid.ColumnDefinitions>

            <!--=======================================================
            1️ Hamburger Menu
        =========================================================-->
            <Button x:Name="HamburgerButton"
                Grid.Column="0"
                Background="Transparent"
                BorderThickness="0"
                Command="{x:Bind VM.ToggleMenuCommand}"
                ToolTipService.ToolTip="{StaticResource TopBar_HamburgerButton.ToolTip}">
                <FontIcon x:Name="HamburgerIcon"
                      Glyph="&#xE700;"
                      Style="{StaticResource TopBarIconStyle}" />
            </Button>

            <!--=======================================================
            2️ Back Button
        =========================================================-->
            <Button x:Name="BackButton"
                Grid.Column="1"
                Background="Transparent"
                BorderThickness="0"
                ToolTipService.ToolTip="{StaticResource TopBar_BackButton.ToolTip}">
                <FontIcon x:Name="BackIcon"
                      Glyph="&#xE72B;"
                      Style="{StaticResource TopBarIconStyle}" />
            </Button>

            <!--=======================================================
            3️ Page Title + Breadcrumb
        =========================================================-->
            <StackPanel x:Name="PageTitlePanel"
                    Grid.Column="3"
                    Orientation="Vertical"
                    VerticalAlignment="Center"
                    Spacing="2"
                    ToolTipService.ToolTip="{StaticResource TopBar_PageTitlePanel.ToolTip}">
                <TextBlock x:Name="HeadingText"
                       Text="{x:Bind VM.Heading, Mode=OneWay}"
                       Style="{StaticResource TopBarHeadingTextStyle}" />

                <TextBlock x:Name="BreadcrumbText"
                       Text="{x:Bind VM.BreadCrumpText, Mode=OneWay}"
                       Style="{StaticResource TopBarBreadcrumbTextStyle}" />
            </StackPanel>

            <!--=======================================================
            4️ User Info – name, surname, session + avatar
        =========================================================-->
            <Grid x:Name="UserInfoGrid"
              Grid.Column="5"
              Background="{StaticResource UserInfoBackgroundBrush}"
              VerticalAlignment="Center"
              Padding="{StaticResource UserInfoPadding}"
              ToolTipService.ToolTip="{StaticResource TopBar_UserInfo.ToolTip}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <controls:PersonPicture x:Name="UserPicture"
                                    Grid.Column="0"
                                    Width="{StaticResource UserPictureSize}"
                                    Height="{StaticResource UserPictureSize}"
                                    Margin="0,0,8,0" />

                <StackPanel x:Name="UserDetailsPanel"
                        Grid.Column="1"
                        Orientation="Vertical"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="UserNameText"
                           Text="{x:Bind VM.UserName}"
                           Foreground="White"
                           FontSize="{StaticResource UserInfoFontSize}" />
                    <TextBlock x:Name="UserSurnameText"
                           Text="{x:Bind VM.UserSurname}"
                           Foreground="White"
                           FontSize="{StaticResource UserInfoFontSize}" />
                    <TextBlock x:Name="SessionDurationText"
                           Text="{x:Bind VM.SessionDuration}"
                           Foreground="White"
                           FontSize="{StaticResource UserInfoSecondaryFontSize}" />
                </StackPanel>
            </Grid>

            <!--=======================================================
            5️ Notifications Button (Temporarily Disabled)
            TODO: Hook to INotificationService when ready.
        =========================================================-->
            <Border Grid.Column="6"
                CornerRadius="{StaticResource DefaultCornerRadius}"
                BorderBrush="{StaticResource DisabledBorderBrush}"
                BorderThickness="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                ToolTipService.ToolTip="{StaticResource TopBar_NotificationButton.ToolTip}">
                <Button x:Name="NotificationButton"
                    IsEnabled="True"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="{StaticResource NotificationButtonPadding}">
                    <FontIcon x:Name="NotificationIcon"
                          Glyph="&#xEA8F;"
                          Style="{StaticResource TopBarIconStyle}" />
                </Button>
            </Border>

        </Grid>

    </Border>

</UserControl>
<!--
=====================================================================
📐 TopBar.xaml – Visual Structure Overview
=====================================================================

This file defines the top-level header bar used across all pages
of the tOrder application. It includes primary user interface
elements for navigation, page context, and user session.

The component is designed to be placed inside a `Viewbox` and 
scaled proportionally using centralized layout configuration 
(`LayoutConfigVM.LayoutScale`).

🧩 Root Structure:
────────────────────────────────────────────
UserControl (TopBar)
└── Border (TopBarBorder)
    └── Grid (TopBarGrid)
        ├── Button (HamburgerButton)
        ├── Button (BackButton)
        ├── StackPanel (PageTitlePanel)
        ├── Grid (UserInfoGrid)
        └── Button (NotificationButton)

────────────────────────────────────────────

📘 UI Element Breakdown:
────────────────────────────────────────────

1️⃣ Button: HamburgerButton
    - Left-aligned menu toggle icon (☰).
    - Bound to `VM.ToggleMenuCommand`.

2️⃣ Button: BackButton
    - Optional back navigation icon.
    - Shown conditionally or always depending on context.

3️⃣ StackPanel: PageTitlePanel
    - Displays current page title and breadcrumb.
    - Bound to `VM.Heading` and `VM.BreadCrumpText`.

4️⃣ Grid: UserInfoGrid
    - Shows logged-in user info, picture and session time.
    - Uses `PersonPicture` and text fields bound to:
        → `VM.UserName`, `VM.UserSurname`, `VM.SessionDuration`.

5️⃣ Button: NotificationButton
    - Right-aligned notification bell icon.
    - Intended for future alert/notification system.

────────────────────────────────────────────

🧩 Design & Scaling:
────────────────────────────────────────────

- The control assumes a fixed design width (1024px) and height (68px),
  specified via `common:DesignProperties`.

- To achieve layout consistency across resolutions, it is recommended
  to wrap this control inside a `Viewbox` and bind scale from:

      → `LayoutConfigVM.LayoutScale`

- The component scales proportionally with the rest of the shell.

────────────────────────────────────────────

✅ Summary
────────────────────────────────────────────

TopBar is a reusable, scalable shell-level header component.
It displays page context, user session data, and basic commands.

It follows the same layout configuration model as `MainLayout.xaml`,
ensuring consistent appearance across resolutions and DPI settings.
Designed to work with MVVM and centralized layout control.
-->
