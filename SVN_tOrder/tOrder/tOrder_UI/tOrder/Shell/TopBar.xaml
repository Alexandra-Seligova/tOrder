<!-- 
    TopBar komponenta
    ──────────────────
    Záhlaví aplikace zobrazující:
    - Hamburger menu (pro otevření bočního panelu)
    - Tlačítko zpět
    - Název stránky a breadcrumb navigaci
    - Informace o uživateli a čase
    - Ikonu oznámení

    Výška (v návrhu): 68 px (common:DesignProperties.DesignHeight)
    Přizpůsobení velikosti pomocí globálního vertikálního ScaleTransform.
-->

<UserControl
    x:Class="tOrder.Shell.TopBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.Shell"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:common="using:tOrder.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:DataType="local:TopBarVM">

    <Border x:Name="TopBarBorder"
            Background="#003f7f"
            VerticalAlignment="Top">

        <Grid x:Name="TopBarGrid"
              Height="68"
              Padding="8,0"
              ColumnSpacing="20"

              common:DesignProperties.DesignHeight="68"
              common:DesignProperties.GroupTag="Layout"
              common:DesignProperties.GroupSubTag="Header">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Hamburger -->
                <ColumnDefinition Width="Auto"/>
                <!-- Back button -->
                <ColumnDefinition Width="Auto"/>
                <!-- Reserved -->
                <ColumnDefinition Width="*"/>
                <!-- Page title + breadcrumb -->
                <ColumnDefinition Width="Auto"/>
                <!-- Reserved -->
                <ColumnDefinition Width="Auto"/>
                <!-- User info -->
                <ColumnDefinition Width="Auto"/>
                <!-- Notification -->
            </Grid.ColumnDefinitions>

            <!-- 1️⃣ Hamburger Menu -->
            <Button Grid.Column="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{x:Bind VM.ToggleMenuCommand}">
                <FontIcon Glyph="&#xE700;" FontSize="28" Foreground="White"/>
            </Button>

            <!-- 2️⃣ Back Button -->
            <Button Grid.Column="1"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Back">
                <FontIcon Glyph="&#xE72B;" FontSize="28" Foreground="White"/>
            </Button>

            <!-- 3️⃣ Page Title + Breadcrumb -->
            <StackPanel Grid.Column="3"
                        Orientation="Vertical"
                        VerticalAlignment="Center"
                        Spacing="2">
                <TextBlock Text="{x:Bind VM.Heading, Mode=OneWay}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock Text="{x:Bind VM.BreadCrumpText, Mode=OneWay}"
                           FontSize="14"
                           Foreground="White"/>
            </StackPanel>

            <!-- 4️⃣ User Info (User Picture + Session Details) -->
            <Grid Grid.Column="5"
                  Background="#052f60"
                  VerticalAlignment="Center"
                  Padding="8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <controls:PersonPicture Grid.Column="0"
                                        Width="36"
                                        Height="36"
                                        Margin="0,0,8,0"/>

                <StackPanel Grid.Column="1"
                            Orientation="Vertical"
                            VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind VM.UserName}" Foreground="White" FontSize="12"/>
                    <TextBlock Text="{x:Bind VM.UserSurname}" Foreground="White" FontSize="12"/>
                    <TextBlock Text="{x:Bind VM.SessionDuration}" Foreground="White" FontSize="10"/>
                </StackPanel>
            </Grid>

            <!-- 5️⃣ Notifications Icon -->
            <Button Grid.Column="6"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Notifications">
                <FontIcon Glyph="&#xEA8F;" FontSize="28" Foreground="White"/>
            </Button>

        </Grid>
    </Border>
</UserControl>
