<Page
    x:Class="tOrder.UI.CapacityUnitDashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.UI"
    xmlns:content="using:tOrder.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="UnitGreenBrush" Color="#7FBE92"/>
        <SolidColorBrush x:Key="HeaderBlueBrush" Color="#708CB4"/>
    </Page.Resources>

    <Grid Background="#F5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Tab Selector -->
            <RowDefinition Height="*"/>
            <!-- Main content -->
        </Grid.RowDefinitions>

        <local:DashboardHeadC Grid.Row="0" />
        <local:TabSelectorView x:Name="TabSelectorView" Grid.Row="1"/>
        <ContentPresenter x:Name="DashboardContentPresenter" Grid.Row="2"/>
    </Grid>
</Page>
