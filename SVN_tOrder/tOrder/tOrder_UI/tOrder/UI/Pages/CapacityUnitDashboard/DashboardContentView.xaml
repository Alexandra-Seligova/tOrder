<UserControl
    x:Class="tOrder.UI.DashboardContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <UserControl.Resources>


    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Main 3 columns -->
            <RowDefinition Height="Auto"/>
            <!-- Info row  Footer buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Viewbox Grid.Row="0" x:Name="DashboardContentHeaderViewbox" Stretch="Uniform" Height="Auto">
            <Grid x:Name="DashboardContentHeaderContainer" Width="1280">
                <Grid.RenderTransform>
                    <ScaleTransform x:Name="DashboardContentHeaderScale" />
                </Grid.RenderTransform>

                <local:DashboardContentHeaderC  Height="96"/>
            </Grid>
        </Viewbox>
        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <!-- adaptivní horní prostor -->
                <RowDefinition Height="Auto" />
                <!-- uzamčený Viewbox obsah -->
                <RowDefinition Height="*" />
                <!-- adaptivní spodní prostor -->
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Stretch="Uniform" Height="Auto">
                <Grid Width="1280" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- header -->
                        <RowDefinition Height="*"/>
                        <!-- main 3 columns -->
                        <RowDefinition Height="Auto"/>
                        <!-- footer -->
                    </Grid.RowDefinitions>

                    <!-- 📦 Hlavní obsah – 3 sloupce -->
                    <Grid x:Name="MainContentGrid" Grid.Row="1" Height="264">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Levý port -->
                        <local:DashboardContentPortC 
                            Width="300"
                        Grid.Column="0"
                        HeaderText="Eingang Behälter" 
                        OutputText="Eingang 251.:" 
                        OutputTypeText="" 
                        ButtonText="Behälter öffnen zur&#x0a;Bearbeitung"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                            Side="Left"
                            />

                        <!-- Prostřední obrázek -->
                        <Grid Grid.Column="1" Background="#ECECEC">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <!-- Ikona vstupu -->
                            <Image Grid.Column="1"
           Source="ms-appx:///Assets/PortInput.svg"
           Stretch="Uniform"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           Width="60"
           Height="60"/>

                            <!-- Obrázek stroje -->
                            <Image Grid.Column="2"
           Source="ms-appx:///Assets/DashboardMachine.png"
           Stretch="Uniform"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"/>

                            <!-- Ikona výstupu -->
                            <Image Grid.Column="3"
           Source="ms-appx:///Assets/PortOutput.svg"
           Stretch="Uniform"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           Width="60"
           Height="60"/>
                        </Grid>



                        <!-- Pravý port -->
                        <local:DashboardContentPortC 
                             Width="300"
                                 Side="Right"
                        Grid.Column="2"
                        HeaderText="Ausgang Behälter" 
                        OutputText="Ausgang 251.:" 
                        OutputTypeText="Produktion" 
                        ButtonText="Behälter abschließen &#x0a;nach der Bearbeitung"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"/>
                    </Grid>
                </Grid>
            </Viewbox>
        </Grid>

        <!-- Header -->
        <Viewbox Grid.Row="2" x:Name="DashboardContentFooterViewbox" Stretch="Uniform" Height="Auto">
            <Grid   x:Name="DashboardContentFooterContainer" Width="1280">
                <Grid.RenderTransform>
                    <ScaleTransform x:Name="DashboardContentFooterScale" />
                </Grid.RenderTransform>


                <local:DashboardContentFooterC  Margin="0,0,0,0" Padding="0,0,0,0"/>
            </Grid>
        </Viewbox>



    </Grid>
</UserControl>