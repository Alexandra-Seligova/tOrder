<UserControl
    x:Class="tOrder.UI.DashboardContentFooterC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Proměnné pro pohodlné ladění -->
        <x:Double x:Key="FooterButtonMinWidth">140</x:Double>
        <x:Double x:Key="FooterButtonHeight">38</x:Double>
        <x:Double x:Key="FooterButtonFontSize">16</x:Double>
        <CornerRadius x:Key="FooterButtonCornerRadius">4</CornerRadius>
        <Thickness x:Key="FooterButtonMarginThickness">6,0,6,0</Thickness>
        <x:Double x:Key="FooterButtonViewboxHeight">22</x:Double>
    </UserControl.Resources>

    <Grid>
        <Grid HorizontalAlignment="Center" Margin="0,16,0,0" Width="950" Padding="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Šedá tlačítka -->
            <Button Grid.Column="0" Background="#888"
                Foreground="White" FontWeight="SemiBold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock Text="Rüsten Ende" TextAlignment="Center"/>
                </Viewbox>
            </Button>
            <Button Grid.Column="1" Background="#888"
                Foreground="White" FontWeight="SemiBold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock Text="Muster Freigabe" TextAlignment="Center"/>
                </Viewbox>
            </Button>
            <!-- Modrá tlačítka -->
            <Button Grid.Column="2" Background="#19599A"
                Foreground="White" FontWeight="Bold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock Text="Produktionsstart" TextAlignment="Center"/>
                </Viewbox>
            </Button>
            <Button Grid.Column="3" Background="#19599A"
                Foreground="White" FontWeight="Bold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock Text="Auftragsverwaltung" TextAlignment="Center"/>
                </Viewbox>
            </Button>
            <Button Grid.Column="4" Background="#19599A"
                Foreground="White" FontWeight="Bold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock TextAlignment="Center">
                        Neue Produktions-
                        <LineBreak/>
                        unterbrechung
                    </TextBlock>
                </Viewbox>
            </Button>
            <Button Grid.Column="5" Background="#19599A"
                Foreground="White" FontWeight="Bold"
                FontSize="{StaticResource FooterButtonFontSize}"
                MinWidth="{StaticResource FooterButtonMinWidth}"
                Height="{StaticResource FooterButtonHeight}"
                Padding="0,6" BorderThickness="0"
                Margin="6,0,6,0"
                CornerRadius="{StaticResource FooterButtonCornerRadius}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Viewbox Height="{StaticResource FooterButtonViewboxHeight}" Stretch="Uniform">
                    <TextBlock Text="Produktionsreport" TextAlignment="Center"/>
                </Viewbox>
            </Button>
        </Grid>
    </Grid>
</UserControl>
