<UserControl
    x:Class="tOrder.UI.DashboardContentPortC"
    x:Name="RootControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.UI"
    xmlns:common="using:tOrder.Common"
    xmlns:conv="using:tOrder.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <UserControl.Resources>
        <!-- Box pro hodnoty -->
        <Style x:Key="PortHeaderBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="#BDBDBD"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Padding" Value="2,2"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- Hodnoty uvnitř boxů -->
        <Style x:Key="PortHeaderValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Popisky -->
        <Style x:Key="PortHeaderLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Popisky menší (pro Menge nebo přípony) -->
        <Style x:Key="PortHeaderSubLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Hodnota menší (pro množství) -->
        <Style x:Key="PortHeaderSubValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="PortBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="#CCCCCC"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Padding" Value="4,2,4,2"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="40"/>
        </Style>
        <Style x:Key="PortValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style x:Key="TabButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#154A84"/>
            <Setter Property="BorderBrush" Value="#154A84"/>
            <Setter Property="BorderThickness" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="TabButtonUnselectedStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Popisek (label) -->
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Hodnota (value) -->
        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Přípona (např. Stk.) -->
        <Style x:Key="UnitTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>


        <conv:TabButtonStyleConverter x:Key="TabButtonStyleConverter"
                                      SelectedStyle="{StaticResource TabButtonSelectedStyle}"
                                      UnselectedStyle="{StaticResource TabButtonUnselectedStyle}" />
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

    </UserControl.Resources>
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <!-- Title Row -->
            <RowDefinition Height="40"/>
            <!-- Tabs Row -->
            <RowDefinition Height="40"/>
            <!-- Content Row -->
            <RowDefinition Height="*"/>
            <!-- Button Row -->
            <RowDefinition Height="Auto"/>
            <!-- free -->
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- 🔷 Nadpis portu -->
        <Grid Grid.Row="0" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text=""
                   Grid.Column="0"
                   FontFamily="Segoe MDL2 Assets"
                   FontSize="22"
                   Foreground="#2196F3"
                   Margin="6,0,0,0"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Right"/>

            <TextBlock Text="{x:Bind HeaderText, Mode=OneWay}"
                   Grid.ColumnSpan="2"
                   FontWeight="Bold"
                   FontSize="18"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
        </Grid>

        <!-- 🏷️ Taby -->
        <Grid Grid.Row="1" Height="40">
            <ItemsRepeater x:Name="TabRepeater"
                       ItemsSource="{x:Bind PortContainers, Mode=OneWay}">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Horizontal"/>
                </ItemsRepeater.Layout>

                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="common:PortContainerM">
                        <Button Click="TabButton_Click"
                            Tag="{x:Bind ContainerId}"
                            Loaded="TabButton_Loaded"
                            Height="40"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Width="{Binding ElementName=RootControl, Path=TabWidth}">

                            <TextBlock x:Name="TabLabel"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                            <Button.Style>
                                <Binding Converter="{StaticResource TabButtonStyleConverter}"
                                     Path="ContainerId"
                                     ConverterParameter="{Binding ElementName=RootControl, Path=SelectedContainerId}"/>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </Grid>
        <!-- 🧾 Obsah -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ⬅️ Řádek 1 - Ausgang nebo Eingang -->
            <TextBlock Grid.Row="1" Grid.Column="1"
               Text="{x:Bind OutputText}"
               Style="{StaticResource LabelTextStyle}"
               Margin="4,4,6,5"/>

            <Border Grid.Row="1" Grid.Column="2"
            Style="{StaticResource PortHeaderBoxStyle}"
            Margin="0,0,0,5">
                <TextBlock Text="{x:Bind ContainerModel.ContainerId, Mode=OneWay }"
                   Style="{StaticResource PortHeaderValueStyle}"/>
            </Border>

            <!-- 🏷️ Řádek 2 - Typ -->
            <TextBlock Grid.Row="2" Grid.Column="1"
               Text="Typ:"
               Style="{StaticResource LabelTextStyle}"
               Visibility="{x:Bind IsRightPort, Converter={StaticResource BoolToVisibilityConverter}}"
               Margin="4,4,6,5"/>


            <TextBox Grid.Row="2" Grid.Column="2"
                            Text="{x:Bind OutputTypeText}"
/>


            <!-- ⬇️ Řádek 3 - Menge -->
            <TextBlock Grid.Row="3" Grid.Column="1"
               Text="Menge:"
               Style="{StaticResource LabelTextStyle}"
               Margin="4,4,6,5"/>

            <Border Grid.Row="3" Grid.Column="2"
            Style="{StaticResource PortHeaderBoxStyle}"
            Margin="0,0,0,5">
                <TextBlock Text="{x:Bind ContainerModel.PieceCount, Mode=OneWay}"
                   Style="{StaticResource PortHeaderValueStyle}"/>
            </Border>

            <TextBlock Grid.Row="3" Grid.Column="3"
               Text="Stk."
               Style="{StaticResource UnitTextStyle}"
               Margin="6,4,4,5"/>
        </Grid>




        <!-- 🟦 Tlačítko -->
        <Grid Grid.Row="3" x:Name="PortActionContainer">
            <Button Background="#2196F3"
                Foreground="White"
                FontWeight="SemiBold"
                Margin="0,8,0,8"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Padding="10,6"
                BorderThickness="0"
                CornerRadius="4"
                MinWidth="140"
                IsEnabled="{x:Bind IsActionEnabled, Mode=OneWay}"
                Click="{x:Bind OnCloseContainer}">
                <TextBlock Text="{x:Bind ButtonText, Mode=OneWay}" TextAlignment="Center" TextWrapping="Wrap"/>
            </Button>
        </Grid>

    </Grid>

</UserControl>
