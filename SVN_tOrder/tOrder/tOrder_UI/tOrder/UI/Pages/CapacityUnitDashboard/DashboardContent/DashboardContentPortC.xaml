<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="tOrder.UI.DashboardContentPortC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tOrder.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
 Width="240" MinHeight="280">

    <UserControl.Resources>
        <Style x:Key="PortBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="#CCCCCC"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="0,2"/>
        </Style>
        <Style x:Key="PortValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style x:Key="TabButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#154A84"/>
            <Setter Property="BorderBrush" Value="#154A84"/>
            <Setter Property="BorderThickness" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="TabButtonUnselectedStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#B5B5B5" BorderThickness="1" CornerRadius="4" Background="#F7F7F7" Padding="0">
        <StackPanel>
            <!-- Tab buttons přes celou šířku -->
            <Grid Background="#F7F7F7" Margin="0" Height="32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="1" Grid.Column="0" Style="{StaticResource TabButtonSelectedStyle}"/>
                <Button Content="2" Grid.Column="1" Style="{StaticResource TabButtonUnselectedStyle}"/>
                <Button Content="3" Grid.Column="2" Style="{StaticResource TabButtonUnselectedStyle}"/>
                <Button Content="4" Grid.Column="3" Style="{StaticResource TabButtonUnselectedStyle}"/>
                <Button Content="5" Grid.Column="4" Style="{StaticResource TabButtonUnselectedStyle}"/>
            </Grid>


            <!-- Main content -->
            <StackPanel Margin="8,0,8,8" Spacing="8">
                <!-- Header -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="4" Margin="0,6,0,0">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="#2196F3" Margin="0,0,3,0"/>
                    <TextBlock Text="Eingang Behälter" FontWeight="Bold" FontSize="18"/>
                </StackPanel>

                <!-- Ausgang -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Ausgang 1." FontSize="16"/>
                    <Border Style="{StaticResource PortBoxStyle}" Margin="16,0,0,0" MinWidth="40" HorizontalAlignment="Right">
                        <TextBlock Text="8" Style="{StaticResource PortValueStyle}" FontStyle="Italic"/>
                    </Border>
                </StackPanel>

                <!-- Hlavní počet kusů -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                    <Border Style="{StaticResource PortBoxStyle}" MinWidth="120">
                        <TextBlock Text="6.540" Style="{StaticResource PortValueStyle}"/>
                    </Border>
                    <TextBlock Text="Stk." FontSize="18" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Tlačítko -->
                <Button
    Background="#2196F3"
    Foreground="White"
    FontWeight="SemiBold"
    Margin="0,20,0,0"
    HorizontalAlignment="Center"
    Padding="10,6"
    BorderThickness="0"
    CornerRadius="4"
    MinWidth="140">
                    <TextBlock TextAlignment="Center">
        Behälter öffnen zur
        <LineBreak/>
        Bearbeitung
                    </TextBlock>
                </Button>

            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>